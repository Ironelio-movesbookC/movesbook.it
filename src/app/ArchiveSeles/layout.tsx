import ClubWorkspaceShell from '@/components/club/ClubWorkspaceShell';

export default function ArchiveSalesLayout({ children }: { children: React.ReactNode }) {
  return <ClubWorkspaceShell>{children}</ClubWorkspaceShell>;
}
