'use client';

/** @deprecated Use `useEntityDirectAccessGuard` from the same module path. */
export {
  useEntityDirectAccessGuard as useClubDirectAccessGuard,
  useEntityDirectAccessLocked as useClubDirectAccessLocked,
  useEntityDirectAccessLockedForKind,
} from '@/hooks/useEntityDirectAccessGuard';
